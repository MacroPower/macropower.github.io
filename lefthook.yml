# Run `lefthook install -f` once after cloning, or enter `devbox shell` which
# does it automatically.

pre-commit:
  parallel: true
  commands:
    typecheck:
      glob: "{tsconfig.json,themes/ubuntu-unity/assets/js/**/*.{ts,tsx}}"
      run: npx --no-install tsc --noEmit
