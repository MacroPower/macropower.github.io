{
  "name": "jacobcolvin-com",
  "private": true,
  "description": "Personal Hugo site for jacobcolvin.com. Type-checks the ubuntu-unity theme's TypeScript sources; Hugo's esbuild does the actual bundling.",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
